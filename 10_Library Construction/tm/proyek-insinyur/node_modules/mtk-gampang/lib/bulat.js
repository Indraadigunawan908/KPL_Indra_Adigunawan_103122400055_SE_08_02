/**
 * Mengubah bilangan non-bulat menjadi bilangan bulat dengan membuang bagian desimal (truncate).
 * @param {number} x - Bilangan yang akan dibulatkan
 * @returns {number} Bilangan bulat hasil truncate
 */
export function bulat(x) {
    return Math.trunc(x);
}
