/**
 * Menghitung nilai x pangkat y.
 * @param {number} x - Basis
 * @param {number} y - Eksponen
 * @returns {number} Hasil x pangkat y
 */
export function pangkat(x, y) {
    return Math.pow(x, y);
}
